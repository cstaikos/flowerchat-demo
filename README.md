# Vue.js with Express starter template


### Info
This project supports:
- live reloading -> Browser-Sync
- auto build -> Gulp


### Installation
- Run `npm init` to install dependencies 
- Run `gulp` from directory to start express server + browser-sync
